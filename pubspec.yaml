name: agendemais
description: Sistema SaaS de agendamento para clínicas e barbearias
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^12.1.3
  http: ^1.1.0
  flutter_riverpod: ^2.4.9
  flutter_dotenv: ^5.0.2
  google_fonts: ^5.1.0
  intl: ^0.18.1
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - .env