name: agenda_facil
description: "SaaS de agendamento online para autônomos"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # AWS Amplify
  amplify_flutter: ^2.0.0
  amplify_auth_cognito: ^2.0.0
  amplify_api: ^2.0.0
  amplify_storage_s3: ^2.0.0
  
  # State Management
  flutter_riverpod: ^2.4.9
  
  # Navigation
  go_router: ^12.1.3
  
  # HTTP Client
  http: ^1.1.2
  dio: ^5.4.0
  
  # Local Storage
  shared_preferences: ^2.2.2
  
  # Date/Time
  intl: ^0.19.0
  
  # UI Components
  cupertino_icons: ^1.0.6
  
  # Utils
  uuid: ^4.2.1
  
  # Charts
  fl_chart: ^0.68.0
  
  # Web-compatible alternatives
  # pdf: ^3.10.7 # Removed for web compatibility
  # path_provider: ^2.1.2 # Removed for web compatibility  
  # mailer: ^6.0.1 # Removed for web compatibility
  
  # Web-compatible email service
  js: ^0.6.7
  universal_html: ^2.2.4
  
  # Logging
  logger: ^2.0.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true