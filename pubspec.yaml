name: flutter_application_1
description: AGENDEMAIS - Sistema de Agendamento SaaS
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  http: ^1.2.1
  path_provider: ^2.1.3
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  go_router: ^14.0.2
  qr_flutter: ^4.1.0
  freezed_annotation: ^2.4.1

  # Pacotes adicionados para funcionalidades existentes
  fl_chart: ^0.68.0
  pdf: ^3.10.8
  printing: ^5.12.0
  table_calendar: ^3.1.1

  # Firebase - Adicionado para a nova arquitetura de backend
  firebase_core: ^2.30.1
  firebase_auth: ^4.19.2
  cloud_firestore: ^4.17.0
  firebase_storage: ^11.7.2
  firebase_analytics: ^10.10.3 # Opcional, para analytics

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.9
  freezed: ^2.5.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo.png
