version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands:
            - echo "Serving Flutter Web files from web directory"
            - ls -la web/
      artifacts:
        baseDirectory: web
        files:
          - '**/*'