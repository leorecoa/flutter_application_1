version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands:
            - echo "Using pre-built files"
      artifacts:
        baseDirectory: web
        files:
          - '**/*'