version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands:
            - echo "Serving redirect page to GitHub Pages"
            - cp web/redirect.html web/index.html
      artifacts:
        baseDirectory: web
        files:
          - '**/*'