version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands:
            - chmod +x build_flutter.sh
            - ./build_flutter.sh
      artifacts:
        baseDirectory: build/web
        files:
          - '**/*'
      cache:
        paths:
          - flutter/**/*
          - ~/.pub-cache/**/*