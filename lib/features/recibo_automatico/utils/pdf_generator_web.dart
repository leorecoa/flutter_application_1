// Web-compatible PDF generator placeholder
import 'dart:typed_data';

class PDFGenerator {
  static Future<Uint8List> gerarReciboPDF(dynamic recibo) async {
    // Web implementation would use pdf-lib or similar
    print('PDF generation not available on web platform');
    return Uint8List(0);
  }
}