{
  "name": "agenda-facil-backend",
  "version": "1.0.0",
  "description": "AgendaFácil SaaS Backend",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "aws-sdk-client-mock": "^3.0.0",
    "eslint": "^8.49.0",
    "jest": "^29.7.0",
    "jest-mock": "^29.7.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1467.0"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "lambda/**/*.js",
      "!lambda/**/node_modules/**"
    ],
    "coverageDirectory": "coverage",
    "coverageReporters": ["text", "lcov", "html"]
  }
}